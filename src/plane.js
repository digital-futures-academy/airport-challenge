class Plane {
    constructor(name, flyingStatus){
        this.name = name;
        this.flyingStatus = flyingStatus;
    }

    // Need to add a method here to allow the plane to change its internal flying status property. 

    // Also consider adding a location of the plane here as a property as well as a setter method to set the location. 

}

module.exports = Plane;