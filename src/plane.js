class Plane {
    constructor(id) {
        this.id = id;
    }
}

// const airport = require("./airport");

// const plane = { id: `F-15A` };

// console.log(airport.airportPlane);

// // class {

// // }

module.exports = {
    Plane,
}