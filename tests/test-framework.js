const assertEquals = (input1, input2) => { return input1 === input2 };

//testing number on left to if it's less than number on right
//this may need some parsing as a int before making comparison
const assetLessThan = (input1, input2) => { return input1 < input2 };


module.exports = {
    assertEquals,

}