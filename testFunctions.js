// Testing functions for export and use in tests e.g. function.spec.js

// test comment on already staged file

const assertEquals = (val1, val2) => val1 === val2;

module.exports = {
    assertEquals
}