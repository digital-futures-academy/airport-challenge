const { airport } = require('.../src/airport')


/*let input, expectedOutput, actualOutput, result;
// --------------------------------------------------
console.log("countEventNumbersInArray function test");
// --------------------------------------------------
console.log("Testing that [1,2,3,4,5,6] => 3");
// 1. Setup/Arrange
input = [1, 2, 3, 4, 5, 6];
expectedOutput = 3;

// 2. Execute/Act
actualOutput = countEvenNumbersInArray(input);

// 3. Verify/Assert
result = assertEquals(actualOutput, expectedOutput);
console.log(result);

As an air traffic controller
So I can get passengers to a destination
I want to instruct the airport to land a plane
*/

const assertEquals = (landed, planeLanded) => {
    return landed === planeLanded
}

const assertEquals = (stormy, weatherConditions) => {
    return stormy === weatherConditions
}

const assertEquals = (Full, airportCapacity) => {
    return
}
module.exports = assertEquals;